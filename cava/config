[general]
bars = 0
framerate = 60
autosens = 1
bar_width = 2
bar_spacing = 1
max_height = 100
; sleep_timer = 1
center_align = 1

# Frequency range (music-friendly)
lower_cutoff_freq = 50
higher_cutoff_freq = 10000


[input]
method = pulse
source = auto


[output]
# Use ncurses for best compatibility in terminals
method = ncurses
orientation = bottom

# Mono gives a cleaner frequency sweep left → right
channels = mono
mono_option = average
reverse = 0

# Optional: show frequency labels
xaxis = none


# Show a subtle baseline when idle
show_idle_bar_heads = 1


[color]
# Leave as default so it follows your terminal theme
background = default
foreground = "#f2cdcd"

# Uncomment for gradient coloring (optional)
gradient = 1
gradient_count = 4
gradient_color_1 = '#abace8'
gradient_color_2 = '#f2d5cf'
gradient_color_3 = '#f2d5cf'
gradient_color_4 = '#eebebe'

[smoothing]
# Modern replacement for integral / gravity
noise_reduction = 77


[eq]
# Flat EQ (recommended)
1 = 1
2 = 1
3 = 1
4 = 1
5 = 1

